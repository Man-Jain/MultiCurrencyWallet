version: "3"
services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: npm run start
    # volumes:
    # - .:/app
    ports:
      - "9001:9001"
    stdin_open: true

volumes:
  db-data:
